{
  "name": "vitpapers-frontend",
  "private": true,
  "scripts": {
    "build": "npx tsc",
    "watch": "npx tsc --watch",
    "dev": "npm run build && npx serve dist"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
